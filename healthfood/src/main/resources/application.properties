spring.application.name=healthfood

# -------------------------------# Configuration de la base de données# -------------------------------
spring.datasource.url=jdbc:postgresql://localhost:5432/healthfood_db
spring.datasource.username=postgres
spring.datasource.password=1518112003

# -------------------------------# JPA / Hibernate# -------------------------------
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# -------------------------------# Port du serveur# -------------------------------#
server.port=8081

spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

jwt.secret=healthfood_super_secure_key_2026_dev_only_very_long_key_123456789